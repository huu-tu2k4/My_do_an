# My_do_an

Environment variables

Create a `.env` file in the BackEnd folder (not committed) with at least:

- `JWT_SECRET` — secret used to sign JWT tokens (example in `.env.example`).

See `.env.example` for an example.
